# 給一般使用者：下載後怎麼用（Windows）

GitHub 上看到的 `.py`、`.md` **就是程式**（原始碼），不是空白網站。  
你要的是：**下載整個資料夾 → 雙擊 bat 執行**，步驟如下。

---

## 1. 下載專案（二選一）

### 方法 A：從 GitHub 下載 ZIP（最簡單）

1. 打開倉庫頁面：  
   **https://github.com/Bala7651/Discord-Bot-Media-Downloader**
2. 點綠色按鈕 **Code**
3. 點 **Download ZIP**
4. 下載後用小老鼠（或 Windows）**解壓縮**到任意資料夾  
   例如：`C:\Users\你的名字\Downloads\Discord-Bot-Media-Downloader-main`

### 方法 B：從 Releases 下載（若作者有發佈）

1. 倉庫頁面右側 **Releases**
2. 下載最新的 `Discord-Bot-Media-Downloader-xxx.zip`
3. 解壓縮

---

## 2. 安裝 Python（只要裝一次）

1. 開啟：https://www.python.org/downloads/
2. 下載並安裝 **Python 3.10 或更新**
3. 安裝畫面**務必勾選**：
   - ✅ **Add python.exe to PATH**
4. 完成後**重新開機**或至少重開命令視窗（建議）

---

## 3. 啟動程式

1. 進入解壓後的資料夾  
2. 雙擊 **`run_gui.bat`**
3. **第一次**會自動：
   - 建立虛擬環境 `.venv`
   - 安裝需要的套件（可能要等 1～3 分鐘，請勿關閉黑窗）
4. 之後會跳出**圖形介面**

若黑窗一閃就關或出現錯誤：
- 看同資料夾的 `launch.log`
- 確認已安裝 Python 並勾選 PATH

---

## 4. 第一次在介面裡做什麼

1. 準備 Discord **Bot Token**（Developer Portal → Bot → Copy）
2. 開啟 **Message Content Intent**
3. Bot 要有「檢視頻道」「讀取訊息歷史」
4. 貼上 Token 與 Channel ID → **開始備份**

介面有**首次教學**可一步步看。

---

## 常見問題

### GitHub 上只有一堆文字，是不是壞了？

沒壞。程式就是這些文字檔。  
使用者要 **Download ZIP 解壓後執行**，不是在網頁上按一個「啟動」。

### 需要會寫程式嗎？

不需要。會解壓縮、雙擊 `run_gui.bat`、貼上 Token 即可。

### 要不要上傳 `.venv`？

不要。每個人電腦不同，第一次跑 bat 會自動建自己的 `.venv`。

### macOS / Linux？

沒有 bat，請在終端機：

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python gui.py
```
